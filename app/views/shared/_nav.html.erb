<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
  <nav class="my-0 mr-md-auto">
      <%= link_to 'Order', root_path, class: "p-2" %>
      <%= link_to 'Categories', categories_path, class: "p-2" if logged_in?(:site_admin) %>
      <%= link_to ('Open orders'), open_orders_path, class: "p-2" if logged_in?(:user, :site_admin) %>
      <%= link_to 'Ready orders', payment_orders_path, class: "p-2" if logged_in?(:user, :site_admin) %>
  </nav>
  <nav class="my-2 my-md-0 mr-md-3">
      <%= link_to "Logout", destroy_user_session_path, method: :delete, class:"btn btn-danger" if logged_in?(:user, :site_admin) %>
      <%= link_to (fa_icon "shopping-cart 2x"), cart_path, class:"p-2" %>
  </nav>
      <h5><%= link_to current_cart.items_count, cart_path, class: "badge badge-pill badge-primary" %></h5>
</div>
